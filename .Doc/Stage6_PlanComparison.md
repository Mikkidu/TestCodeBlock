# ЭТАП 6: Выбор стратегии выполнения команд

**Дата:** 24 декабря 2025
**Статус:** Требуется выбор подхода перед реализацией
**Влияние:** На архитектуру всех последующих этапов

---

## 📊 Сравнение вариантов

### Вариант A: "Y-позиция" ❌ ОТКЛОНЕН

**Суть:** Сортируем блоки сверху вниз по Y-координатам, связываем в этом порядке.

```csharp
var sorted = blocksInProgram.OrderByDescending(b => b.RectTransform.y).ToList();
for (int i = 0; i < sorted.Count - 1; i++)
    sorted[i].Command.Next = sorted[i + 1].Command;
```

**Проблемы:**
```
IF условие
├─ Yes → Block A (внизу экрана)
└─ No  → Block B (вверху экрана)

По Y-позиции: Block B → Block A
Логически: нужны 2 разных пути

❌ Y-сортировка не может это представить
```

| Аспект | Статус |
|--------|--------|
| Условные ветвления | ❌ Невозможно |
| Параллельные потоки | ❌ Невозможно |
| Множественные выходы | ❌ Невозможно |
| Будущие параметры | ❌ Не подходит |
| Группировка блоков | ❌ Нет информации |
| Переработка позже | ⚠️ **Почти гарантирована** |

**Вывод:** Тупиковый вариант, приведет к рефакторингу.

---

### Вариант B: "Соединения" (Рекомендуется) ✅

**Суть:** Блоки напрямую связаны через соединения BlockConnector ↔ BlockConnector.

```csharp
// Block A output → Block B input (физическое соединение)
blockA.outputPoints[0].connectedTo = blockB.inputPoints[0];

// При выполнении: идем по соединению
BlockUI next = currentBlock.GetNextBlock();  // через connectedTo
```

**Архитектура:**
```
BlockConnector (визуальная точка + логическое соединение)
    ├─ pointType: Input/Output
    ├─ visualElement: кружок на экране
    ├─ parentBlock: ссылка на владельца
    └─ connectedTo: ← НОВОЕ! ссылка на следующий BlockConnector

При snap:
    targetOutput.connectedTo = inputPoint  // создано соединение!

При выполнении:
    BlockUI next = currentBlock.GetNextBlock()
    // внутри: return outputPoints[0].connectedTo.parentBlock
```

**Структура для 7 этапов:**
```
Этап 1-4: ✓ Уже выполнены (визуальная часть работает)

Этап 5: ✓ Выполнен (snap уже создает физическое выравнивание)
    + Добавить логику соединения (connectedTo = ...)

Этап 6: Реализовать соединения (Stage 6 v2)
    + parentBlock для навигации
    + connectedTo для связи

Этап 7: Параметры (готова инфраструктура)
    + ParameterType в BlockConnector готов
    + Соединения могут хранить параметры позже
```

| Аспект | Статус |
|--------|--------|
| Условные ветвления | ✅ Легко |
| Параллельные потоки | ✅ Легко |
| Множественные выходы | ✅ Легко (outputPoints[0], [1], [2]...) |
| Будущие параметры | ✅ Встроено |
| Группировка блоков | ✅ Идеально (идем по соединениям) |
| Рефакторинг | ✅ Не требуется |

**Преимущества:**
- ✅ Логика соответствует архитектуре
- ✅ Визуальное = логическое (что видим, то выполняем)
- ✅ Масштабируется
- ✅ Совместимо с Этапом 7 (параметры)
- ✅ Готово для мультипотока (если будет)

**Недостатки:**
- Немного больше кода чем вариант А (но обоснованно)
- Нужно добавить `DisconnectOutput()` для разрыва связей

**Время реализации:** 45-60 минут

---

### Вариант C: "Явные соединения" (BlockConnection объекты) ⏳

**Суть:** Отдельный класс Connection который хранит связь.

```csharp
public class BlockConnection
{
    public BlockConnector sourceOutput;
    public BlockConnector targetInput;
    public bool isActive = true;
}
```

| Аспект | Статус |
|--------|--------|
| Явность кода | ⭐⭐⭐ Максимум |
| Флексибильность | ✅✅✅ Максимум |
| Сложность | ⚠️⚠️⚠️ Высокая |
| Время на Stage 6 | ⏳ 1.5-2 часа |

**Вывод:** Хорош для будущего, но слишком усложняет Stage 6. Лучше сделать позже (Phase 2).

---

## 🎯 РЕКОМЕНДАЦИЯ

### Выберем: **Вариант B (Соединения BlockConnector)**

**Почему:**
1. ✅ Исходная архитектура предполагала связанный список
2. ✅ Соответствует плану Stage 1-7
3. ✅ Простая реализация (добавить 2 поля в BlockConnector)
4. ✅ Полностью совместима с дальнейшим развитием
5. ✅ Логика соответствует визуальной репрезентации
6. ✅ Готово для группировки блоков (управление по соединениям)

**Путь реализации:**

```
Stage 6 (СЕЙЧАС):
├─ BlockConnector: добавить parentBlock + connectedTo
├─ BlockUI.InitializeConnectors(): установить parentBlock
├─ SnapManager.ApplySnap(): создавать connectedTo
├─ BlockUI.GetNextBlock(): возвращать следующий блок
└─ CommandExecutor: выполнять по соединениям (не по Y)

Stage 7 (позже):
├─ BlockConnector: ParameterType (уже есть!)
├─ BlockConnection: Вариант C если нужна флексибильность
└─ Мгарция с явными объектами соединений

Stage 8+ (будущее):
├─ Параллельные потоки
├─ Условные ветвления (If/Else)
├─ Передача параметров
└─ Группировка блоков по соединениям
```

---

## 📝 Конкретный план Stage 6

### Список изменений:

**1. BlockConnector.cs**
```diff
  public class BlockConnector
  {
      public enum PointType { Input, Output }

      public PointType pointType;
      public RectTransform visualElement;
+     public BlockUI parentBlock;           // ← НОВОЕ
+     public BlockConnector connectedTo;    // ← НОВОЕ

      public BlockConnector(PointType type, RectTransform visual)
      {
          pointType = type;
          visualElement = visual;
      }
  }
```

**2. BlockUI.InitializeConnectors()**
```diff
  // При создании каждого connector
  BlockConnector inputConnector = new BlockConnector(...);
+ inputConnector.parentBlock = this;
  inputPoints.Add(inputConnector);
```

**3. SnapManager.ApplySnap()**
```diff
  public void ApplySnap(BlockUI draggingBlock,
                        BlockConnector inputPoint,
                        BlockConnector targetOutput)
  {
      // ... выравнивание позиции ...
+     targetOutput.connectedTo = inputPoint;
+     Debug.Log($"[CONNECTION] {targetOutput.parentBlock.name} → {inputPoint.parentBlock.name}");
  }
```

**4. BlockUI.GetNextBlock()**
```csharp
public BlockUI GetNextBlock()
{
    if (outputPoints.Count > 0 && outputPoints[0].connectedTo != null)
    {
        return outputPoints[0].connectedTo.parentBlock;
    }
    return null;
}

// Опционально:
public void DisconnectOutput(int outputIndex = 0)
{
    if (outputIndex < outputPoints.Count)
    {
        outputPoints[outputIndex].connectedTo = null;
    }
}
```

**5. CommandExecutor / Execution Logic**
```diff
- // ДО: по Y-позиции
- var sorted = blocksInProgram.OrderByDescending(b => b.RectTransform.y).ToList();
- for (int i = 0; i < sorted.Count - 1; i++)
-     sorted[i].Command.Next = sorted[i + 1].Command;

+ // ПОСЛЕ: по соединениям
+ BlockUI currentBlock = startBlock;
+ while (currentBlock != null)
+ {
+     yield return ExecuteCommand(currentBlock.Command);
+     currentBlock = currentBlock.GetNextBlock();  // идем по физической связи
+ }
```

### Этапы реализации:
1. Обновить BlockConnector (+5 мин)
2. Обновить InitializeConnectors (+10 мин)
3. Обновить ApplySnap (+10 мин)
4. Добавить GetNextBlock (+5 мин)
5. Обновить CommandExecutor (+15 мин)
6. Тестирование (+10 мин)
7. Документация (+5 мин)

**Итого: ~60 минут**

---

## ❓ Вопросы для подтверждения

1. **Согласны ли вы с выбором Варианта B (соединения BlockConnector)?**
   - Да → идем реализовывать Stage 6
   - Нет → обсудим альтернативу

2. **Нужно ли сейчас реализовать `DisconnectOutput()`?**
   - Да → добавим в Stage 6
   - Позже → сделаем как отдельную задачу

3. **Где находится логика выполнения блоков (CommandExecutor)?**
   - Нужно найти файл и обновить
   - Или она в GameManager?

4. **Нужны ли логи соединений при snap?**
   - Да, `[CONNECTION]` логи помогут отладке
   - Нет, убирать

---

## Итоговое резюме

```
ВЫБОР: Вариант B - Соединения через BlockConnector ✅

ПОЧЕМУ:
- Соответствует исходной архитектуре (связанный список)
- Простая реализация
- Масштабируется без переработки
- Готово для If/Else и параметров

КОГДА: Stage 6, после согласования с вами

КАК:
- Добавить 2 поля в BlockConnector
- Обновить 4 файла
- 60 минут работы
- Тестирование на месте

РЕЗУЛЬТАТ:
- Выполнение блоков = логика соединений
- Готово для Stage 7 и выше
- Группировка блоков (будущее)
- Полная совместимость
```

---

## Готово начинать реализацию?

Пожалуйста, подтвердите:
1. ✅ Вариант B выбран
2. ✅ Согласны обновить логику выполнения
3. ✅ DisconnectOutput() - позже или сейчас?

После подтверждения сразу начну реализацию! 🚀
